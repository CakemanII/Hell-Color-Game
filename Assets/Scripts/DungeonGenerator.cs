using System.Collections.Generic;
using UnityEngine;

public class DungeonGenerator : MonoBehaviour
{
   /* public static DungeonGenerator Instance { get; private set; }

    [SerializeField] private GameObject entrance;

    [SerializeField] private List<GameObject> rooms;

    [SerializeField] private List<GameObject> specialRooms;

    [SerializeField] private List<GameObject> alternateEntrances;

    [SerializeField] private List<GameObject> hallways;

    [SerializeField] private GameObject door;


    [SerializeField] private int noOfRooms = 10;
    [SerializeField] private LayerMask roomsLayermask;

    private List<DungeonPart> generatedRooms;
    private bool isGenerated = false;

    private void Awake()
    {

    }

    private void Start()
    {

    }

    public void StartGeneration()
    {
        StartGenerationServerRpc();

    }

    [ServerRpc(RequireOwnershp = false)]
    private void StartGenerationServerRpc()
    {
        Generate
        GenerateAlternateEntrances();
        FillEmptyEntrances();
        isGenerated = true;
    }

    priavte void Generate()
    {
        for i = 0; isGenerated < noOfRooms - alternateEntrances.Count; i++)
        {
            if (generatedRooms.Count < 1)
            {
                GameObject generatedRooms = AsyncInstantiateOperation(entrance, transform.position, Transform.rotation);
            }
        }
    }

    */

}
